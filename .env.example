# Copy to .env and fill in to enable "MAKE PACKAGE & UPLOAD" uploads.
# Without these, packaging still works; only the MCP upload step is skipped.
CR_MCP_SERVER=https://<server>
CR_MCP_TOKEN=crmcp_...
